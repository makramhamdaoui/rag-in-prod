embedding:
  model_path: "sentence-transformers/all-mpnet-base-v2"
  asymmetric: false
  dimension: 384

chunking:
  chunk_size: 300
  overlap: 100

ollama:
  model_name: "qwen3:8b"

logging:
  file_path: "logs/app.log"

opensearch:
  host: "localhost"
  port: 9201
  index: "documents"

postgres:
  url: "postgresql://rag_user:rag_password@localhost:5433/rag_db"
